# Copy to .env and fill in values before running.
# docker compose up --build

# LLM provider key (required once AI review is wired up)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# SQLite path used outside Docker; Compose overrides this for the container.
DATABASE_URL=file:./data/dev.db
