{
  "id": "off-by-one",
  "title": "Sum array with off-by-one",
  "language": "python",
  "code": "def sum_values(values):\n    total = 0\n    for i in range(len(values) + 1):\n        total += values[i]\n    return total\n",
  "expectations": [
    {
      "category": "BUG",
      "severityAtLeast": "WARNING",
      "lineNear": 3,
      "descriptionIncludes": ["index", "range", "off-by", "bound", "len(", "out of"]
    }
  ]
}
